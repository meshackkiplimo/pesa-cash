# Required Environment Variables
PORT=5000
NODE_ENV=development
MONGO_URI=your-mongodb-connection-string
JWT_SECRET=your-jwt-secret

# M-Pesa Configuration (Required for payment features)
MPESA_CONSUMER_KEY=your-consumer-key
MPESA_CONSUMER_SECRET=your-consumer-secret
MPESA_SHORTCODE=your-shortcode
MPESA_PASSKEY=your-passkey
MPESA_ENVIRONMENT=sandbox
BASE_URL=http://localhost:5000

# Email Configuration (Required for password reset functionality)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-specific-password
EMAIL_FROM=noreply@pesacash.com

# Default Admin Credentials (Used for initial admin setup)
ADMIN_EMAIL=admin@investpro.com
ADMIN_PASSWORD=Admin@123
ADMIN_FIRST_NAME=Admin
ADMIN_LAST_NAME=User

# Note: Replace all "your-*" values with actual values.
# Do not commit the actual .env file to version control.
# Make sure to change the default admin password after first login.